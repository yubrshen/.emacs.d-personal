;; This is a note of customization to make ediff works to ignore the difference in white space to be able to find
;; similarity in structure excluding white space.

 ;; '(ediff-custom-diff-program "diff")
 ;; '(ediff-diff-options "--binary -b")
 ;; '(ediff-diff-program "diff")
 
;; Currently the setting is done in custome.el, so here it's just used for recording purpose. 
